【7.3.4】
每一个“父进程”在打印输出后进程终止，子进程成为孤儿进程
从第2到第20个“子进程”的“父进程”的进程id都是1

【7.3.5】
每一个“父进程”在打印其自身输出信息前，都会因为wait()调用而阻塞，进程未终止
并等待“子进程”（id号比它大的进程）调用结束的状态返回
最后一个“子进程”无等待“子进程”（id号比它大的进程）调用结束的状态返回，打印输出
其他进程按照创建先后顺序由后往前依次终止进程、返回状态，打印输出，父进程进程id号为创建它的进程的进程id号

【7.3.6】
每一个“父进程”在打印其自身输出信息后，都会因为wait()调用而阻塞，进程未终止
并等待“子进程”（id号比它大的进程）调用结束的状态返回
最后一个“子进程”打印输出，无等待“子进程”（id号比它大的进程）调用结束的状态返回
所以进程按照创建先后顺序打印输出，父进程进程id号为创建它的进程的进程id号
按照创建先后顺序由后往前依次终止进程、返回状态

【7.3.8】
在7.3.7中，因为prtastr()函数在向write()调用的缓冲区中写入字符串个数的n值与命令行参数输入的n值/时延n值相同
可能会导致输出不出语句后面的“子进程”的“父进程”的进程号

比较运行结果：
与7.3.5、7.3.6的分析基本相同
不同的是因为每个进程调用prtastr()函数而导致的输出结果截断

【7.3.10】
在7.3.9中，若“父进程”A创建“子进程”B，"父进程"B创建“子进程”C，则环的相连应为A->B->C->A
在7.3.9中，若“父进程”A创建“子进程”B，"父进程"A创建“子进程”C，则环的相连应为A->C->B->A
但在各“子进程”退出时会造成环的断裂

假设进程环个数为5
“父进程”A创建“子进程”B，两者各向管道中读写
程序执行快，“父进程”A同时又创建“子进程”C、D、E
B，在从管道获得A的值后，无“子进程”，打印输出，状态返回		输出process1的是它
A，在从管道获得B的值后，wait()不阻塞，打印输出，进程终止		因为已经循环创建需要个数的“子进程”，则其的循环个数id号最大为5
B，从管道获取一次A的值后结束进程
A，从管道获取一次B的值后，在每一次创建“子进程”C、D、E的时候又从管道中读出B的值加入数组
“子进程”C、D、E由于A“父进程”的结束成为孤儿进程
B、C、D、E是否能够建立管道、读写数据、打印输出、阻塞取决于时序、“父进程”创建“子进程”数量
目前现象是：D建立管道并从A读到它创建“子进程”时写入的进程id号

【7.3.11】
每个“父进程”在创建“子进程”“同时”，“父子进程”继续创建
“父子进程”的创建、是否能够建立管道、读写数据、打印输出、阻塞取决于时序取决于时序和数量
程序执行快，多个进程共享最后的进程数量id
与7.3.10分析基本相同

进程的数目大于nprocs，且随着nprocs增大，进程数目增大更快
调整成什么样？

【修改】
在ringchildbreak.c文件中
//wait()调用
    pid_t childpid1;
    childpid1 = wait(NULL);
    if (childpid1 != -1)
    {
        fprintf(stderr,"Waited for child with pid %ld\n", childpid1);//漏打1    标准输出已被重定向！
    }

已改ring1.c


【注意点】
各个进程的读写时序取值问题